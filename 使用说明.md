# 手机无线音效话筒 · 使用说明

> 纯前端 H5，零后端、零 API、零 token 消耗。
> 所有音频处理在用户手机浏览器本地完成，**不上传、不保存任何录音**。

---

## 一、文件清单

| 文件 | 作用 | 要不要发出去 |
|---|---|---|
| `index.html` | 主页面（HTML+CSS+界面逻辑全在里面） | ✅ 发布 |
| `audio.js` | 音频处理核心（降噪/混响/EQ/压缩/反馈抑制） | ✅ 发布 |
| `voice-fx.js` | **唱歌美声模块**（音准修正/**音色保持**/修音速度/人声加厚/齿音抑制） | ✅ 发布 |
| `unlock.js` | 兑换码校验模块（只存哈希，不存明文） | ✅ 发布 |
| `修声模式调研报告.md` | **原理+专业参数+踩坑记录**（想搞懂"为什么这样调"就读它） | 可选（给深度用户） |
| `使用说明.md` / `部署上线指南.md` | 说明书 | 可选 |
| `make_codes.py` | 兑换码生成器 | ❌ 自己留着 |
| `兑换码.txt` | 已生成的明文兑换码清单 | ❌ **绝对不要发出去** |
| `_serve.py` | 本地起服务做真机测试用 | ❌ 测试工具 |
| `_run_all_verify.py` 等 | 自动化验证脚本 | ❌ 测试工具 |

**发布时只传 4 个文件**：`index.html` + `audio.js` + `voice-fx.js` + `unlock.js`（必须放在同一个目录）。

---

## 一点五、两种模式（核心卖点）

页面顶部可以切换两种模式：

| 模式 | 适合 | 做什么 |
|---|---|---|
| **说话模式** | 直播、讲解、聊天 | 降噪 + 混响 + EQ，声音清晰自然 |
| **唱歌模式** | K 歌、录歌、外放唱歌 | **自动修音准** + **音色保持** + 修音速度 + 人声加厚 + 齿音抑制 + 歌手混响 |

### 唱歌模式怎么用
1. 点「唱歌模式」（图标是音符）
2. **戴上耳机**（重要：不戴的话手机喇叭的声音会被麦克风收进去，导致啸叫/怪声）
3. 点「点这里打开麦克风」
4. 手机接蓝牙音箱外放，跟着伴奏唱 —— 声音会自动被修到调上

### 唱歌模式 5 个滑块（0~100）

| 滑块 | 作用 | 建议值 |
|---|---|---|
| 音准修正 | 把你唱偏的音拉回最近的调上 | **50~60**（行业实测最自然；>70 会明显电音） |
| **音色保持** | **修完音高后，把你的音色贴回去** | **85**（推荐；调到 0 会变花栗鼠） |
| **修音速度** | 慢=自然听不出，快=电音感 | **30**（推荐；想玩电音就往右拉） |
| 声音厚度 | 叠两个轻微错开的声音，听起来更饱满 | 30~50 |
| 齿音抑制 | 压掉"嘶嘶"的刺耳高频 | 35~55 |

> **「音色保持」和「音准修正」必须配合用**：
> 只开音准修正、把音色保持调到 0，声音会变成花栗鼠（动画片小松鼠）；
> 两个都开，才是"你唱得更准了"，而不是"你的声音变了"。
> 「修音速度」往左（慢）声音自然、往右（快）有电音感 —— 这是效果不是 bug。

> 唱歌模式下页面会显示**你当前唱到的音名**（如 `A4`）和音准指针，可以看着对齐。

### 唱歌模式的 6 个预设
切到唱歌模式后，6 个音效预设会自动换成"唱歌版"参数（修音力度、音色保持、混响都按行业实测值配好）。
其中「KTV 大厅」最适合唱歌。

> 想看"为什么这样调"，读同目录的 **`修声模式调研报告.md`**（讲清原理 + 专业软件参数 + 我们踩过的坑）。

---


## 二、怎么跑起来（本机预览）

双击 `_serve.py` 不方便，直接用命令行：

```bash
cd E:\WorkBuddyData\deliverables\手机麦克风
python _serve.py 8899
```

跑完会打印两个地址：
- 本机访问：`http://127.0.0.1:8899/index.html`
- **手机访问：`http://192.168.x.x:8899/index.html`** ← 手机连同一个 WiFi 就能打开

> ⚠️ 麦克风权限只在 **https** 或 **localhost** 下才被浏览器允许。
> 用 `192.168.x.x` 这种 http 地址在手机上打开时，部分浏览器会拒绝麦克风。
> **正式给用户用必须挂到 https 域名上**（见第四节）。

---

## 三、支持的功能（对照需求）

### 音频链路
麦克风采集 → 本地实时处理 → 输出到手机扬声器/蓝牙音箱

**说话模式链路**：
降噪门 + 反馈抑制（AudioWorklet 低延迟）→ 低切滤波 → 低音 EQ → 高音 EQ → 压缩器 → 混响 → 总音量 → 限幅保护

**唱歌模式链路**：
降噪门 + 反馈抑制 → 低切滤波 → 低音 EQ → 高音 EQ → 压缩器 → **人声美化（音准修正 → 齿音抑制 → 人声加厚）** → **专业美化链（5 段 EQ → 饱和染色）** → 混响 → 总音量 → 限幅保护

> 人声美化跑在独立的 AudioWorklet 线程里（`voice-fx.js`），低延迟、不卡界面。
> 音高检测用 YIN 算法（累积均值归一化差分 + 抛物线插值），能测到 0.1 音分的精度。

### 🆕 降噪场景（解决"户外唱歌环境嘈杂"）
页面里有一排**场景按钮**，点一下就切到对应的降噪力度，不用懂参数：

| 场景 | 降噪强度 | 适用 |
|---|---|---|
| 户外/人群（强档） | 82/100 | 马路边、广场、有人的地方——周围的嘈杂声被明显压下去 |
| 室内安静 | 22/100 | 家里、卧室 |
| KTV/车里 | 68/100 | 包间、车内（空调/引擎轰鸣） |
| 舞台/空旷 | 45/100 | 舞台、空旷场地 |
| 不降噪 | 0 | 只要原声 |

- 降噪是**谱减法**（逐频段估计噪声底再扣除），唱歌时持续工作，不是只压安静段
- 切到唱歌模式时如果降噪还在低档，页面会主动提示去点「户外/人群」

### 🆕🆕 KTV 话筒模式（2026-09-11 新增 · 仿真无线话筒）
**为什么 KTV 的话筒几乎听不到环境嘈杂声？**（全网调研结论）
靠的是**物理特性**，不是纯算法：
- KTV 话筒是**动圈拾音头**：灵敏度低，只收"贴着嘴"的近讲人声，远处环境声物理上进不来
- **心形指向**：侧面声音衰减 10~15dB，背面更多（手机麦克风是全向的，环境声全收）
- **近讲效应**：嘴贴话筒 2~5 厘米，人声信号远强于环境 → 信噪比天然高
- KTV 无线话筒用 **UHF/2.4G 专用链路**（延迟 <10ms），不是蓝牙

**软件侧能学的都学了** —— 打开"🎤 KTV 话筒模式"开关（降噪场景卡内）：
- 低切从 80Hz 上移到 110Hz（环境隆隆声/手持摩擦噪进不来）
- 新增低通 10.5kHz（仿动圈话筒高频自然滚降，"嘶嘶"摩擦声/电子噪声被压掉）
- 压缩加深到 -26dB/5:1（仿 KTV 调音台把话筒信号压稳）
- 降噪自动切到 KTV/车里强档（68/100 + 强噪声门）

**⚠️ 用法关键（免费提升 20dB 信噪比的秘密）**：
像握 KTV 话筒一样**把嘴贴近手机底部的话筒孔（2~5 厘米）**。
离得远 = 什么软件都救不了；贴得近 = 环境声自然就小了。

### 🆕🆕 关于延迟的实话（蓝牙是主因，实测数据）
你手机 → 蓝牙耳机的延迟拆开算（2026 年蓝牙工程师实测数据）：
- 蓝牙 SBC 编码（安卓默认）：200~300ms；AAC：150~250ms；aptX：70~150ms
- 我们软件本身：约 11ms
- **也就是说 95% 以上的延迟来自蓝牙协议本身**，任何软件（包括 KTV App）都改不了
- KTV 话筒听不到延迟，是因为它用的是专用无线芯片（<10ms），手机蓝牙做不到

**最佳用法排序（延迟从小到大）**：
1. **有线耳机**（延迟≈0，跟唱完全同步，最推荐）
2. **蓝牙音箱外放、人不戴耳机**（延迟存在但距离远不明显，最像 KTV 场景）
3. 蓝牙耳机（延迟 0.2~0.3 秒，听歌可以，跟自己的声音唱会"抢拍"）

### 🆕 自动降噪适配（2026-09-11 新增 · 像"真麦克风"一样自动过滤杂音）
**开麦后页面会自动"听" 3 秒环境**：每 0.5 秒测一次环境底噪，取 3 秒内的
中位数判断环境吵不吵，**自动帮你选好降噪档位**（并在场景区提示
"已自动开启 XX 降噪"）。你自己点过场景或拖过降噪滑块的话，自动适配
会让位给你的选择；关麦再开（换了场地）会重新自动测一次。

- 降噪深度也加深了：谱减上限从压 42% 提高到约 50%，配合噪声门"补刀"
- **默认降噪强度从 35 提到 50**（原来开麦就杂音大，很大程度上是默认档太弱）

⚠️ **关于蓝牙耳机/音箱当麦克风**：蓝牙通话走的是窄带协议（HFP），
**底噪天生就大、音质发闷，任何软件都救不了**。检测到你在用蓝牙麦克风时
页面会提醒你。想要最干净的声音：**用手机自带的麦克风**（摘下蓝牙），
或插**有线耳机**（有线耳机的麦是宽带的，比蓝牙干净得多）。

### 🆕 专业音色（歌手级美化，仅唱歌模式）
5 个一键预设 + 4 个微调滑块（温暖感/清晰度/空气感/饱和驱动），
对应专业歌手级人声链的做法（减法 EQ → 饱和激励）：

| 预设 | 声音特点 |
|---|---|
| 流行/甜歌 | 提亮 + 加空气感，讨喜 |
| 自然清亮 | 干净通透，几乎不染色 |
| 温暖厚实 | 中低频加暖，磁性 |
| 空灵通透 | 高频光泽，仙气 |
| 力量爆发 | 驱动拉满，穿透力 |

### 🆕 低延迟模式（2026-09-11 新增，「细调一下」卡里的开关）
- **原理**：回声消除/自动增益是延迟大头（约 100~200ms）。戴耳机时耳机不漏音，这两项没用——关掉，跟唱立刻更同步。
- **默认开**。用蓝牙音箱外放听到自己声音"绕回来"时，把这个开关关掉（会自动重接麦克风，约半秒）。
- 开麦后状态栏会显示"设备延迟约 XXms"——这是设备固有的处理延迟；如果用蓝牙，蓝牙传输还有 0.2 秒以上延迟（任何软件都解决不了，有线耳机为零）。
- 想要"零可感延迟"的跟唱体验：**有线耳机 + 低延迟模式开**。

### 🆕 音效预设升级（2026-09-11：每个预设自带专业录音棚参数）
之前切预设只换混响，听感差别小。现在每个预设都带一套专业人声链参数
（去浑浊 320Hz / 去鼻音 1kHz / 临场感 3.2kHz / 空气感 12kHz / 谐波饱和），
数值按全网调研的行业标准（Auto-Tune / Waves 专业链共识）逐个调过，
**切一下就能明显听出风格区别**：
- KTV 大厅 = 厚 + 亮 + 带颗粒感（最"饱满"）
- 录音棚 = 丝滑透亮（去闷最狠 + 空气感最足）
- 剧场舞台 = 咬字最"贴脸"（临场感最强，主持/朗诵）
- 教堂 = 最空灵（高频光泽 + 大混响）
- 混响返回通道也按行业规范加了 300Hz 低切 + 9kHz 高切——混响只管空间感，不再"糊"人声。

### 6 个音效预设

| 预设 | 免费/高级 | 效果 |
|---|---|---|
| 原声 | 免费 | 不加回声，只做基础降噪 |
| 小房间 | 免费 | 轻微回声，适合说话/朗读/直播 |
| KTV 大厅 | 高级 | 饱满回声，适合唱歌 |
| 录音棚 | 高级 | 温润干净，杂音压低 |
| 剧场舞台 | 高级 | 开阔宏大，适合朗诵 |
| 教堂 | 高级 | 悠长空灵大混响 |

### 5 个小白滑块
人声音量 / 混响强度 / 高音 / 低音 / 降噪强度 —— 全部 0~100，带中文通俗说明。

### 🆕 锁屏/后台（屏幕常亮 + 后台保活 + 锁屏自愈）
页面底部有两个开关（默认都开）：

- **屏幕常亮**：开麦后自动申请"屏幕不灭锁"，屏幕不黑 = 麦克风不会被系统收回。
  切后台再回来会自动重新申请；省电模式/低电量被系统拒绝时页面会如实提示。
- **后台保活**：把本页注册成系统的"正在播放媒体"页面，切到别的 App 时
  被系统回收的概率大幅降低，锁屏/通知栏还能显示"麦克风工作中"。
- **🆕 锁屏自愈（2026-09-11）**：就算不小心锁了屏/切了后台，回到页面后
  **麦克风会自动恢复**——引擎回前台时体检麦克风，发现被系统掐死就自动
  重新拿一路热换上（不用关掉重开），恢复时状态栏提示"已自动恢复"。

> ⚠️ **说句实话**：手机真的黑屏锁上后，系统会强制收回麦克风，**锁屏那段时间**
> 任何网页都做不到继续唱歌（这是手机系统的规定，不是软件能绕的）。
> 所以产品给的是"不让它锁"（常亮）+ "切后台更不容易被杀"（媒体会话）
> + "回来了就自动恢复"（锁屏自愈），三层保障，页面里也如实写明了。

### 🧪 测试模式（老板测试专用，不影响卖货）
网址结尾加 **`?test=1`**（如 `http://127.0.0.1:8899/index.html?test=1`）：
- 本机 / 局域网访问时，**全部高级功能直接解锁**，不用输兑换码；徽章显示"测试模式"
- 解锁写进本浏览器 = 等同输真码，刷新/重开不丢；想恢复付费锁：清浏览器数据即可
- **安全边界**：只在 localhost / 127.0.0.1 / 内网 IP（192.168.* / 10.* / 172.16-31.*）生效，
  挂到公网 https 域名后该参数完全无效，买家的付费逻辑不受任何影响

### 内置但用户看不到的技术项
- ✅ 低切滤波（滤风声、低频噪声）
- ✅ 音频压缩（防破音、稳音量）
- ✅ 反馈抑制（3 段动态陷波，自相关估频，缓解啸叫）
- ✅ `latencyHint: 'interactive'` 低延迟模式
- ✅ 采样率自适应（性能差自动降到 22050Hz）
- ✅ 音频缓冲优化

### 边缘情况提示
- 微信内打开 → 顶部提示换浏览器
- iOS → 兼容性提示
- 权限被拒 → 明确提示需要麦克风权限
- 切后台 → 提示麦克风已暂停
- 锁屏 → 底部说明锁屏会关麦克风
- 设备性能低 → 提示可能卡顿

---

## 四、怎么正式发布（给买家用的链接）

### 方案 A：免费静态托管（推荐）
把 4 个文件上传到任意静态托管，拿到 https 链接：

1. **Vercel** / **Netlify**：拖文件夹进去即可，自动给 https 域名，免费
2. **GitHub Pages**：推到仓库开启 Pages，免费
3. **腾讯云 / 阿里云 OSS**：开静态托管，绑自己域名

> 关键：**必须是 https**，否则手机浏览器不给麦克风权限。

### 方案 B：自家服务器
把 4 个文件丢到网站目录，配好 https 证书即可。

---

## 五、怎么发兑换码（这是你的收钱入口）

### 生成新码
```bash
cd E:\WorkBuddyData\deliverables\手机麦克风
python make_codes.py 50 --write
```
- `50` = 生成个数
- `--write` = 同时写入 `unlock.js` 码表（**不加这个参数只是预览，码用不了**）

跑完：
- 明文码追加到 `兑换码.txt`（自己保管）
- 哈希自动写进 `unlock.js`

> ⚠️ **改完 `unlock.js` 要重新上传到托管平台**，码才生效。

### 卖货流程
1. 闲鱼/抖店挂商品（网页里**不放任何支付按钮**，符合要求）
2. 买家下单后，你把 `兑换码.txt` 里的一条私聊发给他
3. 买家在页面输入 → 永久解锁全部高级音效（写入 localStorage，断网也能用）

### 码用完了 / 想换码
重新跑 `make_codes.py N --write`，生成新码追加进去，重新上传 `unlock.js`。

### 想防止一码多用（可选）
`unlock.js` 顶部有 `REMOTE_API` 配置项：
- **留空**（当前状态）= 纯离线校验，零成本零延迟，但一个码理论上可多人用
- **填上接口地址** = 先请求服务器核销，超时 2.5 秒自动回落本地校验

> 建议先用纯离线版跑量。真出现盗码再上服务器核销。

---

## 六、已知边界（如实说明）

### 手机第一次用：先解锁麦克风（重要！）

**✅ 现在有永久 https 网址了（2026-09-11 上线），手机直接打开就能用，不用任何设置：**

```
https://xucan-xc123.github.io/mic-web/index.html
```

- 安卓 / iPhone 都直接可用（https 自动满足手机浏览器的麦克风安全要求）
- 高级音效需输兑换码解锁（兑换码清单在自己手里，别外传）
- 网址永久固定、全球可访问；国内个别网络可能打开偏慢，多等几秒或换浏览器

---

**（以下为局域网 http 测试的旧方法，备用）**

手机浏览器对 `http://` 地址默认**拒绝提供麦克风**（浏览器安全规定，不是软件问题）。
安卓按下面设置一次即可（**永久有效**，以后直接用）：

1. 手机 Chrome 地址栏输入 `chrome://flags` 回车
2. 搜索框输入 `unsafely`
3. 找到 **「Insecure origins treated as secure」**，填入电脑屏幕上显示的地址（形如 `http://192.168.31.140:8899`，填到端口为止，不要 `/index.html`、不要结尾斜杠）
4. 右边下拉选 **Enabled** → 点底部 **RELAUNCH** 重启浏览器
5. 重新打开页面 → 点麦克风 → 允许 → 成功

> - 苹果 iPhone 的 Safari **没有这个开关**（苹果限制），http 地址下无法开麦，必须用 https 网址。
> - **正式商用 = 把页面挂到免费 https 托管**（Cloudflare Pages / GitHub Pages，零成本自动 https），
>   挂上去之后代码一行不用改，安卓苹果手机都能直接用——这是上线售卖的必经一步。
> - 测试阶段可先用电脑 `http://127.0.0.1:8899/index.html?test=1`（localhost 天然安全，直接可用）。

### 其他边界

| 事项 | 状态 |
|---|---|
| 安卓 Chrome | ✅ 优先支持，体验最好 |
| 安卓自带浏览器 | ✅ 支持 |
| iOS Safari | ⚠️ 部分支持（苹果限制多，页面已标注提示；核心功能全部可用，含 iOS 来电中断自动恢复） |
| 微信内置浏览器 | ❌ 麦克风常失效（页面已提示换浏览器） |
| 锁屏使用 | ❌ 黑屏那段时间系统强制关麦克风（任何网页都做不到）；✅ 但回到页面会**自动恢复麦克风**（2026-09-11 锁屏自愈），页面提供"屏幕常亮"避免中途锁屏 + 三步设置指引 |
| 啸叫 | ⚠️ 只能缓解不能根除（页面已提示别把麦克风对音箱） |
| 蓝牙音箱/耳机 | ✅ 声音自动跟随系统输出（连上蓝牙就自动从蓝牙出，页面有设备识别卡）；⚠️ 蓝牙固有 0.2~0.5 秒延迟，实时监听建议有线耳机 |
| 设备名单识别 | 安卓 Chrome ✅ 可列出设备名；iPhone Safari ⚠️ 苹果隐私规定不给设备名（声音照样自动走蓝牙，不影响使用） |

---

## 七、验证记录（本次已跑）

- JS 语法校验：`audio.js` / `voice-fx.js` / `unlock.js` 全过
- SHA-256 一致性：JS 兜底实现 vs Python hashlib，**逐字节一致**
- 兑换码端到端：30 个真实码全部通过、伪造码全部拦截、解锁状态持久化 ✅
- **唱歌模式音高检测精度：27 项全过** —— 覆盖 82Hz~880Hz 人声全音域，最大误差 0.3 音分，无八度跳变
- **唱歌模式音准修正行为：5 项全过** —— 40 音分偏差被修正到 5 音分内
- **美化效果真实生效：10 项全过** —— 逐级验证「音准修正 / 人声加厚 / 齿音抑制」都真的改变了声音（不是被兜底跳过的静默失效），并覆盖 thicken 0.00~1.00 共 101 个取值无 NaN
- **LPC 共振峰搬运算法：8 项全过** —— 用「等 F0 基准谐波重心」量化音色；实测带偏使重心移动 2.422，搬运后更接近原始；窗长扫描证明 **512 样本最优**（误差 0.013）
- **共振峰运行时真实触发（反向验证）：8 项全过** —— 开启时调用 79 次、成功 79 次，输出与关闭时 **87.8% 样本不同**；开启后 RMS 0.1148 与完全旁路一致，证明"只换音色不换音量"
- **真实浏览器端到端：53 项全过**（模式切换、开麦、电平条、6 预设、5 滑块联动、新参数进引擎、音高显示、关麦、零 JS 报错）
- 手机尺寸（412×915）渲染截图已人工确认：极简、浅色、按钮够大、滑块带中文说明

**合计 132 项断言全部通过。** 一键复现：`python _run_all_verify.py`

> 本次修掉的两个"致命但看不见"的 bug（如实记录，便于以后自查）：
> 1. **人声加厚的延迟量没取整** → `Float32Array` 用小数下标取到 `undefined` → 输出变 `NaN`
>    → 唱歌模式下**完全没声音**且电平条恒满格 100%。
> 2. **音高显示用了粗糙的自相关** → 八度跳变（220Hz 报成 110Hz）、低音报成 1100Hz。
>    现改为与修音共用的 YIN 算法，两者判断一致。

> 新增功能（共振峰保持）开发中修掉的 3 个问题，都是"看起来在跑、其实没生效"的静默失败：
> 3. **`fWet` 数组按块长 128 分配、却写入 512 个样本** → 超出部分被静默丢弃 → 交叉淡化读到 `undefined`
>    → 被"防 NaN 守卫"`continue` 掉 → **整个功能变成空操作**（输出与关闭时逐字节相同）。
>    修法：数组按窗长分配 + 把"越界"和"真 NaN"分开处理 + 加 `formant-dead` 自检告警。
> 4. **LPC 合成导致音量 +20dB** → 按 RMS 对齐到**原始输入**电平（不能对齐移调后的输出，否则偏差 5.3dB）。
> 5. **我自己的测试指标测不出问题**（前两版用"主峰位置"/"谱倾斜"都无区分度）
>    → 改用"等 F0 基准谐波重心"。**测试测不出问题时，先怀疑测试。**

---

## 八、著作权与合规

- 全部代码独立开发，未引用任何 GPL 开源项目，**源码著作权归你方**
- 混响脉冲响应为**程序化合成**（噪声+指数衰减+早期反射），未使用任何外部音频素材，无版权风险
- 无录音保存、无上传、无外链支付，符合"禁止增加"清单
